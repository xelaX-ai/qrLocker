/** @type {import('next').NextConfig} */
const nextConfig = {
  // Разрешаем внешние изображения (аватары Google)
  images: {
    domains: ["lh3.googleusercontent.com"],
  },
};

export default nextConfig;
